spring:
  application:
    name: authentication-service
server:
  address: '0.0.0.0'
  port: 10061
env:
  url:
    person-service: ${person_service}/personService
    user-service: ${user_service}/userService
    role-service: ${role_service}/roleService
    session-service: ${session_service}/sessionService